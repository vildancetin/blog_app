import React from "react";

const Users = () => {

  // return <div>{params.userId ? <Outlet /> : <div>Users</div>}</div>;
  return (
    <div>
      <div>Users</div>
    </div>
  );
};

export default Users;
